#Jonathan Rizkallah
#10/4/23

#This program prints "hello world"
print('Hello World")

#This program asks the user for their name and greets them
name = input("Enter name ")
print("Welcome", name)

#Only greet me and intructor
name = input("Enter username: ")

if name == "Jonathan":
  print("Hello Jonathan!")
elif name == "Zoe":
  print ("Welcome Zoe!")
else:
  print ("Invalid username")

#Compute the area of a circle while prompting the users to enter the radius and give the user a nice message with the answer

radius = float(input("What is the radius of your circle? "))
pi = 3.14159265
area =  pi * radius * radius

print("Awesome the area of your cirlce is ", area)

#Compute MPG and prompt the user to enter the number of miles driven and number of gallons used

m=(float(input("Enter the number of miles you have driven: ")))
g=(float(input("Enter the number of gallons you have used: ")))

print("You have driven {m} miles and used {g} gallons. Your miles per gallon are eqaul to {m/g}")

#Convert celsius to fahrenheit

c = int(input("Enter Temperature in Celsius: " ))
f = c * (9/5) + 32

print (f"{c} degrees Celsius is {f} degrees Farenheit")

#Create a program that allows a user top insert the day of the week that their vacation starts
on and the number of days their vacation is. Then tell them what day their vacation ends

starting_day = str(input("What day does your vacation start on? "))
length_of_stay = int(input("How many days is your vacation? "))

days = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"]

day = 0

if starting_day == "Sunday":
    day = 0
elif starting_day == "Monday":
    day = 1
elif starting_day == "Tuesday":
    day = 2
elif starting_day == "Wednesday":
    day = 3
elif starting_day == "Thursday":
    day = 4
elif starting_day == "Friday":
    day = 5
elif starting_day == "Saturday":
    day = 6
else: print("invalid")

for i in range(len(days)):
    x = (days[(day + length_of_stay) % len(days)])


if length_of_stay in range (7, 13):
    print(f"You will return from your vacation next {x}. Have a great vacation!")
elif length_of_stay == 14 :
    print(f"You will return from your vacation two weeks from now on {x}. Have a great vacation!")
elif length_of_stay in range (15, 20):
    print(f"You will return from your vacation in two weeks on {x}. Have a great vacation!")
else:
    print(f"You will return from your vacation on {x}. Have a great vacation!")
